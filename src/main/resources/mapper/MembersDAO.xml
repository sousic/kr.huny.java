<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.huny.persistence.member.MembersDAO">
    <insert id="register">
        INSERT INTO members(userid, userpwd, nickname, grade) VALUES (#{userid}, #{userpwd}, #{nickname}, 10)
    </insert>
    <select id="login" resultType="kr.huny.domain.member.MembersVO">
    <![CDATA[
        SELECT * from members where userid = #{userid} and pwdfailcnt <= #{pwdfailcntLimit} and isloginblock = 0
    ]]>
    </select>
    <select id="idChecker" resultType="kr.huny.domain.member.MembersVO">
        SELECT * from members where userid = #{userid}
    </select>
</mapper>