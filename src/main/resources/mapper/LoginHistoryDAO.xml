<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.huny.persistence.member.LoginHistoryDAO">
    <insert id="InsertLoginHisotry">
        INSERT INTO loginHistory(userid, remoteip, result, flag) values (#{userid}, #{remoteip}, #{result}, #{flag})
    </insert>
    <select id="GetLoginHistoryList" resultType="kr.huny.domain.member.LoginHistoryVO">
        select * from loginHistory order by seq desc limit #{startPage}, #{perPageNum}
    </select>
    <select id="GetLoginHistoryListCount" resultType="java.lang.Integer">
        select count(seq) from loginHistory
    </select>
</mapper>